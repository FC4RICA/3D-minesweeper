const LeaderboardButton = ({ onClick }) => (
  <button className='leaderboardButton' onClick={onClick}>
    <span className='material-symbols-rounded'>leaderboard</span>
  </button>
)

export default LeaderboardButton